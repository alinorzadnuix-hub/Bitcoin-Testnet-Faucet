# ✅ مشکل ورود به پنل ادمین - حل شد!

## 🔍 مشکل پیدا شد!

پسورد واقعی در فایل `.env` شما **متفاوت** از `.env.example` است!

### 🔑 اطلاعات ورود واقعی شما:

```
Username: admin
Password: changeme123
```

---

## ✅ راه حل‌ها:

### راه حل 1️⃣: استفاده از پسورد فعلی (ساده‌ترین)

برای ورود به پنل ادمین از این اطلاعات استفاده کنید:

```
http://localhost:3000/admin

Username: admin
Password: changeme123
```

⚠️ **توجه**: این پسورد ضعیف است! حتما آن را تغییر دهید.

---

### راه حل 2️⃣: تغییر پسورد به پسورد قوی‌تر (پیشنهادی)

#### گام 1: ویرایش فایل `.env`

فایل `.env` را باز کنید و این خط را پیدا کنید:

```env
ADMIN_PASSWORD=changeme123
```

آن را تغییر دهید به:

```env
ADMIN_PASSWORD=BitcoinFaucet2025!Secure
```

یا هر پسورد قوی دیگری که می‌خواهید.

#### گام 2: Restart کردن سرور

```powershell
# Stop server
taskkill /F /IM node.exe

# Start server
node server.js
```

یا فقط فایل `start.bat` را دوباره اجرا کنید.

---

### راه حل 3️⃣: استفاده از اسکریپت خودکار

یک اسکریپت ساخته‌ام که به صورت خودکار پسورد را تغییر می‌دهد:

```powershell
.\change-admin-password.ps1
```

---

## 🛠️ اسکریپت تغییر پسورد خودکار

یک فایل جدید ساختم: `change-admin-password.ps1`

**نحوه استفاده:**

```powershell
# اجرا با پسورد پیش‌فرض پیشنهادی
.\change-admin-password.ps1

# یا با پسورد دلخواه
.\change-admin-password.ps1 -NewPassword "YourCustomPassword123!"
```

---

## 📋 اسکریپت کمکی دیگر

### نمایش اطلاعات ورود فعلی:

```powershell
.\show-admin-credentials.ps1
```

این اسکریپت username و password فعلی شما را نمایش می‌دهد.

---

## ✅ تست کنید!

بعد از تغییر پسورد، می‌توانید تست کنید:

```powershell
.\test-login.ps1
```

---

## 🎯 خلاصه:

| مورد | مقدار فعلی | پیشنهاد |
|------|------------|---------|
| Username | `admin` | همین باقی بماند |
| Password | `changeme123` | تغییر به `BitcoinFaucet2025!Secure` |
| وضعیت | ⚠️ ضعیف | ✅ قوی |

---

## 🔐 توصیه امنیتی:

پسورد `changeme123` بسیار ضعیف است! 

**حتما آن را تغییر دهید به:**
- حداقل 12 کاراکتر
- ترکیبی از حروف بزرگ و کوچک
- اعداد
- کاراکترهای خاص (!@#$%^&*)

**پیشنهاد:** `BitcoinFaucet2025!Secure`

---

حالا می‌توانید با پسورد `changeme123` وارد شوید، یا آن را تغییر دهید! 😊
