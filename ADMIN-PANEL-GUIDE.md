# 🎛️ راهنمای کامل پنل ادمین

این راهنما تمام قابلیت‌های پنل ادمین را به صورت کامل توضیح می‌دهد.

---

## 🔑 ورود به پنل ادمین

### مرحله 1: باز کردن صفحه ادمین

```
http://localhost:3000/admin
```

### مرحله 2: وارد کردن اطلاعات ورود

اطلاعات ورود را از فایل `.env` خود دریافت کنید:

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=BitcoinFaucet2025!Secure
```

> ⚠️ **توجه**: حتما پسورد پیش‌فرض را تغییر دهید!

---

## 💰 بخش اول: موجودی کیف پول و واریز

### نمایش موجودی

پنل ادمین به صورت خودکار موجودی کیف پول شما را نمایش می‌دهد:

- **Available Balance**: موجودی فعلی به بیتکوین
- **Wallet Address**: آدرس کیف پول شما
- **دکمه 🔄 Refresh**: تازه‌سازی موجودی

### واریز به کیف پول

برای شارژ کیف پول فاصت:

1. **QR Code**: QR Code آدرس کیف پول شما نمایش داده می‌شود
2. **Copy Address**: روی دکمه Copy کلیک کنید تا آدرس کپی شود
3. **فاصت‌های رایگان**: از لینک‌های زیر تست کوین رایگان بگیرید:
   - https://testnet-faucet.com/btc-testnet/
   - https://bitcoinfaucet.uo1.net/
   - https://coinfaucet.eu/en/btc-testnet/

4. **ارسال**: کوین‌ها را به آدرس نمایش داده شده ارسال کنید

---

## 📤 بخش دوم: ارسال بیتکوین (بدون محدودیت!)

### ویژگی‌های ارسال ادمین:

- ✅ **بدون محدودیت زمانی**: می‌توانید هر زمان که بخواهید ارسال کنید
- ✅ **بدون محدودیت مقدار**: می‌توانید هر مقداری که بخواهید ارسال کنید
- ✅ **یادداشت اختیاری**: می‌توانید یک یادداشت برای تراکنش اضافه کنید

### نحوه استفاده:

1. **Recipient Address**: آدرس گیرنده را وارد کنید
   - باید آدرس تستنت معتبر باشد (شروع با `m`, `n`, `2`, یا `tb1`)

2. **Amount (satoshis)**: مقدار را به ساتوشی وارد کنید
   - استفاده از دکمه‌های سریع:
     - `10k` = 10,000 ساتوشی (0.0001 BTC)
     - `50k` = 50,000 ساتوشی (0.0005 BTC)
     - `100k` = 100,000 ساتوشی (0.001 BTC)
     - `1M` = 1,000,000 ساتوشی (0.01 BTC)

3. **Note (Optional)**: یادداشت دلخواه اضافه کنید

4. **کلیک روی "💸 Send Bitcoin"**

### نتیجه:

پس از ارسال موفق، اطلاعات زیر نمایش داده می‌شود:

- ✅ مقدار ارسال شده (به BTC و ساتوشی)
- ✅ TX Hash (لینک به Block Explorer)
- ✅ کارمزد تراکنش

---

## ⚙️ بخش سوم: تنظیمات فاصت

### تغییر مقدار پرداخت فاصت

می‌توانید مقداری که کاربران از فاصت دریافت می‌کنند را تغییر دهید:

1. **Faucet Payout (satoshis)**: مقدار جدید را وارد کنید
2. **دکمه‌های سریع**:
   - `5k` = 5,000 ساتوشی
   - `10k` = 10,000 ساتوشی
   - `25k` = 25,000 ساتوشی
   - `50k` = 50,000 ساتوشی

3. **کلیک روی "💾 Save Settings"**

> ℹ️ **نکته**: این مقدار بلافاصله اعمال می‌شود و در فایل `.env` ذخیره می‌شود.

---

## 📊 بخش چهارم: آمار تراکنش‌ها

پنل ادمین 6 نوع آمار مهم را نمایش می‌دهد:

### 1. Total Transactions
- تعداد کل تراکنش‌ها (موفق و ناموفق)

### 2. Faucet Claims
- تعداد درخواست‌های موفق از فاصت

### 3. Admin Sends
- تعداد ارسال‌های انجام شده توسط ادمین

### 4. Total Sent (BTC)
- مجموع بیتکوین ارسال شده (به BTC)

### 5. Total Received (BTC)
- مجموع بیتکوین دریافت شده (واریزها)

### 6. Deposits
- تعداد واریزهای شناسایی شده

> 🔄 این آمارها هر 30 ثانیه به صورت خودکار بروز می‌شوند.

---

## 📥 بخش پنجم: اسکنر واریزها

### عملکرد Deposit Scanner:

این ابزار به صورت دستی بلاکچین را اسکن می‌کند و واریزهای جدید به کیف پول شما را شناسایی می‌کند.

### نحوه استفاده:

1. روی دکمه **"🔍 Scan for Deposits"** کلیک کنید
2. منتظر بمانید تا اسکن کامل شود
3. نتیجه نمایش داده می‌شود:
   - `Scan complete. Found X new deposit(s).`

### زمان استفاده:

- وقتی بیتکوین به کیف پول شما ارسال شده اما در موجودی نمایش داده نمی‌شود
- برای بروزرسانی دستی لیست واریزها

---

## 🔍 بخش ششم: فیلتر و جستجو

### فیلتر تراکنش‌ها:

می‌توانید تراکنش‌ها را بر اساس نوع فیلتر کنید:

- **All**: تمام تراکنش‌ها
- **Faucet Claims**: فقط درخواست‌های فاصت
- **Admin Sends**: فقط ارسال‌های ادمین
- **Custom Sends**: فقط ارسال‌های سفارشی (از API عمومی)

### جستجو بر اساس آدرس:

1. آدرس (یا بخشی از آن) را در کادر جستجو وارد کنید
2. روی دکمه **"Search"** کلیک کنید
3. نتایج نمایش داده می‌شود
4. برای پاک کردن جستجو، روی **"Clear"** کلیک کنید

---

## 📝 بخش هفتم: جدول تاریخچه تراکنش‌ها

### ستون‌های جدول:

| ستون | توضیح |
|------|--------|
| **ID** | شماره منحصر به فرد تراکنش |
| **Type** | نوع تراکنش (🎁 Faucet / 💸 Admin / 🔧 Custom / 📥 Deposit) |
| **Address** | آدرس بیتکوین گیرنده |
| **Amount (BTC)** | مقدار ارسال شده |
| **TX Hash** | هش تراکنش (لینک به Block Explorer) |
| **Status** | وضعیت (completed / pending / failed) |
| **Note** | یادداشت (اگر وجود داشته باشد) |
| **Date** | تاریخ و زمان تراکنش |

### رنگ‌بندی وضعیت:

- 🟢 **Completed**: سبز (موفق)
- 🟡 **Pending**: زرد (در انتظار)
- 🔴 **Failed**: قرمز (ناموفق)

### Pagination:

- **← Previous**: صفحه قبل
- **Page X**: شماره صفحه فعلی
- **Next →**: صفحه بعد

> ℹ️ هر صفحه 50 تراکنش نمایش می‌دهد.

---

## 🔄 بروزرسانی خودکار

پنل ادمین به صورت خودکار هر 30 ثانیه موارد زیر را بروز می‌کند:

- ✅ موجودی کیف پول
- ✅ آمار تراکنش‌ها
- ✅ لیست تراکنش‌ها

> 💡 می‌توانید با کلیک روی دکمه "🔄 Refresh" به صورت دستی بروزرسانی کنید.

---

## 🛡️ امنیت

### توصیه‌های امنیتی:

1. ✅ **پسورد قوی**: حتما پسورد پیش‌فرض را تغییر دهید
2. ✅ **HTTPS**: در محیط production از HTTPS استفاده کنید
3. ✅ **Firewall**: پورت پنل ادمین را محدود کنید
4. ✅ **IP Whitelist**: فقط به IP های مشخص اجازه دسترسی دهید

### تغییر پسورد ادمین:

فایل `.env` را ویرایش کنید:

```env
ADMIN_USERNAME=your_new_username
ADMIN_PASSWORD=YourVerySecurePassword123!
```

سپس سرور را restart کنید.

---

## 🔧 عیب‌یابی

### مشکل: نمی‌توانم وارد شوم

**راه‌حل**:
1. مطمئن شوید username و password در `.env` صحیح هستند
2. Local Storage مرورگر را پاک کنید
3. سرور را restart کنید

### مشکل: موجودی نمایش داده نمی‌شود

**راه‌حل**:
1. مطمئن شوید `FAUCET_PRIVATE_KEY` در `.env` تنظیم شده
2. روی دکمه "🔄 Refresh" کلیک کنید
3. اتصال اینترنت را بررسی کنید

### مشکل: واریزها نمایش داده نمی‌شوند

**راه‌حل**:
1. روی "🔍 Scan for Deposits" کلیک کنید
2. منتظر 1-2 تایید بمانید (10-20 دقیقه)
3. مطمئن شوید به آدرس صحیح ارسال کرده‌اید

---

## 📱 دسترسی از راه دور

برای دسترسی به پنل ادمین از کامپیوتر دیگر:

1. **IP سرور را پیدا کنید**:
   ```powershell
   ipconfig
   ```

2. **Firewall را باز کنید**:
   ```powershell
   New-NetFirewallRule -DisplayName "Bitcoin Faucet" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
   ```

3. **از کامپیوتر دیگر دسترسی پیدا کنید**:
   ```
   http://SERVER_IP:3000/admin
   ```

---

## 🎯 نکات و ترفندها

### 1. استفاده از دکمه‌های سریع

برای سرعت بیشتر، از دکمه‌های سریع مقدار استفاده کنید:
- در فرم ارسال: `10k`, `50k`, `100k`, `1M`
- در تنظیمات: `5k`, `10k`, `25k`, `50k`

### 2. کپی آدرس

آدرس کیف پول را با یک کلیک کپی کنید:
- روی دکمه "📋 Copy" کلیک کنید
- آدرس در clipboard قرار می‌گیرد

### 3. مشاهده جزئیات تراکنش

روی TX Hash کلیک کنید تا جزئیات کامل تراکنش در Block Explorer مشاهده شود.

### 4. بروزرسانی سریع

Ctrl + F5 را بزنید تا صفحه بدون cache بارگذاری شود.

---

## 📞 پشتیبانی

اگر مشکلی داشتید:

1. فایل `TROUBLESHOOTING.md` را بررسی کنید
2. لاگ‌های سرور را بررسی کنید
3. Console مرورگر را برای خطاها چک کنید (F12)

---

**پنل ادمین شما حالا آماده است!** 🎉

تمام قابلیت‌های لازم برای مدیریت فاصت بیتکوین تستنت شما در این پنل موجود است.
